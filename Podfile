# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

use_frameworks!


# Library to abstract away access to NSURLSession:
pod 'Alamofire', '~> 3.0'

# This one helps us to deal with UIImage (resize and stuff)
pod 'AlamofireImage', '~> 2.0'

# If you use Alamofire to abstract away NSURLSession,
# why not use something to abstract away the nitty gritty of URLs, parameters, etc?
# pod 'Moya/ReactiveCocoa'

# This helps us to handle JSON 
pod 'SwiftyJSON', :git => 'https://github.com/SwiftyJSON/SwiftyJSON.git'

# Pod that helps creating the hamburger/drawer menu
# pod 'ENSwiftSideMenu'
pod 'KYDrawerController'

# Progress / loading
pod 'MBProgressHUD', '~> 0.9.1'

# Apple's library CommonCrypto does not have an easy way to import into a Swift projetc at the moment.
# Meanwhile, this Library helps us generate the md5 hash. CommonCrypto easy import/support for swift is on Apple's radar :)
# pod 'CryptoSwift', '~> 0.1'

# These Libraries should helps us to create the store register form:
# pod 'XLForm', '~> 3.0'
# pod 'PureLayout'

# This pod bellow may be used to create the tutorial:
# pod 'Presentation', '~> 2.0'

# Form field Validation and Masking/Formatting:
# pod 'Validation'
# pod 'TextField'
# pod 'Formatter'
# pod "VMaskTextField"

# This pod helps us prevent Keyboard issues:
# pod 'IQKeyboardManagerSwift'






target 'BrasilOutlet' do

end

target 'BrasilOutletTests' do

end

target 'BrasilOutletUITests' do

end

